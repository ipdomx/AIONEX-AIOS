# AIOS v1.1.0-alpha.4 — Execution Safety Layer

Adds a dry-run-first execution contract, policy validation, destructive-command detection, allowed filesystem roots, experiment-verification requirements, human approval gates, automatic pre-change snapshots, rollback metadata, and a persistent execution journal.

This release updates existing core files and adds new files. Extract only inside the current AIOS project directory.
