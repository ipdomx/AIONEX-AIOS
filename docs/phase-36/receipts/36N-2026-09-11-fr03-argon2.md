# Phase 36N receipt — FR-03B4 Argon2 maintenance

This candidate updates only Backend `argon2-cffi` to `25.1.0`. Authentication policy and Argon2 parameters remain unchanged. No database schema, provider activation, payment flow, Cloudflare route, MCP configuration or production runtime is changed by this source receipt.

Local evidence: fresh full Backend requirements install PASS; `argon2-cffi=25.1.0`; Argon2id hash/verify PASS; legacy PBKDF2 marked for update; actual `app.core.auth.pwd_context` Argon2id hash/verify PASS using a synthetic test-only SECRET_KEY; runtime pip-audit returned no known vulnerabilities; Core `956/956` PASS. Protected GitHub Backend, Docker, SBOM, dependency-security, CodeQL, browser-boundary and reporting gates remain mandatory before merge.