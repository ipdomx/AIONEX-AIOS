# Phase 36B — Distributed Project Execution Receipt

- Receipt ID: P36-R-36B-20260817
- Batch: 36B — Live distributed project execution and 1000-user admission foundation
- Date/time UTC: 2026-08-16T23:36:43Z
- Objective: Remove the live ProjectExecution single-worker bottleneck by applying the already-proven Phase 23/24 lease, recovery, fencing and horizontal-worker patterns to the PostgreSQL-backed production path; retain tenant isolation and admit a synthetic 1000-job queue without data loss.
- Production gap before change: The production `ProjectExecutionWorker` uses PostgreSQL `FOR UPDATE SKIP LOCKED`, a heartbeat and an opaque lease token, but each worker process executes only one project at a time, has no durable worker registry, no explicit lease-expiry/fencing metadata, no bounded retry/dead-letter state, no tenant-aware fair scheduler, and no retained worker/queue saturation snapshot. The project-execution data volume is shared only on a Docker host, so multi-host workers additionally require RWX/shared evidence storage before real cluster activation.
- Changed paths/services/schemas: Project execution model + Alembic `20260817_0028`; worker/runtime admission services; API configuration and DB engine pool policy; Owner production-runtime API/UI; project-worker dependency/build contract; Compose/Kubernetes Phase 36B scale assets; Phase 36 program/tests/reporting. No existing production schema has been mutated yet.
- Technology/version review and official sources: refreshed to SQLAlchemy `2.0.51`, Alembic `1.18.5`, Selenium `4.46.0`; current production PostgreSQL remains `16.14`; Kubernetes 1.36 review uses latest published patch `1.36.2`. Project Worker image verifies Node `24.18.1`, npm `11.11.0`, Chromium/ChromeDriver `149.0.7827.53`. Official sources: https://docs.sqlalchemy.org/en/20/ ; https://alembic.sqlalchemy.org/en/latest/changelog.html ; https://www.selenium.dev/downloads/ ; https://www.postgresql.org/docs/16/release-16-14.html ; https://kubernetes.io/releases/ ; https://docs.docker.com/reference/compose-file/services/
- Security/privacy/cost/sector review: tenant IDs remain the scheduling isolation boundary; worker/admission telemetry contains no prompt/project/provider secret values; Redis admission stores only opaque random lease IDs and expiry scores and fails closed in production; stale workers cannot commit after fencing rotation; retries are bounded and DLQ is durable; API pooling is capped at 56 pooled connections under a declared 60-connection budget against current PostgreSQL max_connections=100; provider spend remains governed by existing execution budget/idempotency controls.
- Tests and exact pass counts: fresh Full Backend `641 passed, 1 skipped, 0 failed` in `100.26s`; AIOS Core `721 passed` in `27.07s`; Ruff PASS; Mypy PASS across `179` source files; backend verification PASS; pip dependency check PASS; Owner type/lint/Prettier/build PASS (`86/86` pages), Arabic coverage `927/5`; Browser E2E `11/11` PASS; VIP type/lint/build PASS (`115/115` pages); Owner/VIP dependency audits `0 vulnerabilities`; all PR #389 protected GitHub checks PASS including Backend Tests, Production Docker, Browser, CodeQL, Secrets, SBOM, Dependency Security and Phase 36 Reporting.
- Performance/load evidence: two project workflows overlap; actual SIGKILL worker recovery + stale fencing rejection pass; four independent API child processes admit `1000/1000` unique tenant jobs through shared Redis/PostgreSQL without loss/duplicate/cross-tenant collision using the exact pre-merge production shape (`12+2` DB pool, DB budget `60`, Redis pool `18`, local admission `14`, global admission `48`). Final retained run: `p50=0.804s`, `p95=0.990s`, `p99=1.005s`, `max=1.009s`; PostgreSQL logs show zero connection exhaustion/deadlock and zero leftover test DB connections, and the Redis admission lease set returns empty. The Phase 36B isolated p95 `<=1s` enqueue target passes.
- Problems discovered: P36-0003 PostgreSQL connection exhaustion/deadlock in the initial 1000-admission harness; P36-0004 Redis waiter/pool exhaustion; P36-0005 production API DB pool settings were configured but inactive under global NullPool; P36-0006 Owner runtime UI coupled core readiness to supplemental fabric metrics; P36-0007 the P36-0006 fix initially missed the dedicated Prettier gate. P36-0003/0004/0005 are closed for the source/isolated boundary; P36-0006 and P36-0007 are closed by protected Browser/Frontend reruns. All root causes/fixes remain permanently recorded in the master ledger.
- Root causes and why prior safeguards missed them: historical live ProjectExecution was intentionally single-process; Phase 23/24 distribution was isolated; no prior gate combined 1000 submissions, API/Redis/PostgreSQL connection budgets, real process death and live worker fencing. NullPool preserved historical async test compatibility but made short API admission transactions connection-expensive. Phase 36B adds separate test/worker NullPool and production-API bounded pooling plus distributed admission control.
- Fixes and regression tests: explicit PostgreSQL lease owner/expiry + fencing; worker registry/capacity; concurrent worker slots; tenant-aware/resource-class claims; retry/DLQ; Owner metrics/UI; local+Redis fail-closed admission guard; settings invariants for Redis headroom and DB connection budget; production-API-only bounded pooling; actual process-kill recovery; four-process 1000-admission acceptance; migration downgrade/upgrade test; Compose/Kubernetes scale assets.
- PR / merge SHA / protected checks: Phase 36B PR #389 merged into `main` as `061f63dd47093cc3049a0feda8bb6667aeac9f46`. Implementation commit `61edc6d7e5f37a601b72adf6dbad54af5367bac5`, resilience commit `9a96c11`, and formatting-gate commit `e460a94` are included. Every required protected check was PASS at merge, including Production Docker Build. Phase 36A predecessor PR #388 is `c8332d225c335825fe7af6c0dd47d490b133c0e9`.
- Backup / deployment / rollback: PR #389 is merged but Phase 36B production activation has not yet been performed at this checkpoint. Before applying migration `0028` or recreating services, take a fresh production backup and retain rollback evidence. Source rollback is the pre-36B merge parent `c8332d225c335825fe7af6c0dd47d490b133c0e9`; API pooling can be disabled without schema rollback; migration round-trip `0028 -> 0027 -> 0028` is verified in disposable PostgreSQL. Do not downgrade while Phase 36B workers run.
- Live acceptance and before/after metrics: pre-production isolated acceptance passes; production runtime is still unchanged at this checkpoint. Before pooling single-process p95 was `27.143s`; the final four-process exact-shape p95 is `0.990s` (`p50=0.804s`, `p99=1.005s`, `max=1.009s`). Full Backend is `641 passed, 1 skipped`; database and Redis cleanup evidence is clean. Production live before/after metrics remain pending controlled activation.
- External activation gates remaining: controlled production backup/migration/rebuild/same-host worker-scale/live acceptance; real multi-host worker activation additionally requires RWX/shared evidence storage or an object-store evidence backend plus actual hosts/capacity. The isolated Phase 36B `<=1s` admission target is closed; broader mixed-workload 1000+ scale/chaos remains owned by 36N.
- Next action: create and commit this post-merge production-activation checkpoint, verify the current production baseline, create a fresh backup, then apply migration `20260817_0028` and rebuild/recreate only the required services with rollback protection. Activate same-host distributed project-worker replicas only after health checks, collect live queue/fabric metrics, update this receipt/master report, and only then decide whether Batch 36B can be marked complete and current batch advanced to 36C.
