# التقرير النهائي للدفعة — سحب صلاحيات Identity Media — 10 سبتمبر 2026

## الحالة الفعلية

**نُشر الإصلاح واختُبر على الإنتاج بنجاح.** طلب الدمج #603 دُمج بعد نجاح الفحوص الاثني عشر، دون تجاوز حماية الفرع. كود التطبيق المنشور: `9194176c6118e7483d64b05e1183d6c479bfdd3a`.

صورة Backend وعامل Identity Media المنشورة:
`sha256:edcb331a71794832e0d52fb9f74b0cb61242917f1f070299b31d41bf668bf870`.

## ما تم إصلاحه

كانت صلاحية المالك تُراجع عند إنشاء الطلب فقط. أُضيفت مراجعة السلطة الحالية عند التنفيذ المؤجل، والمرحلة الثانية لتوليد الكلام، وتسليم المدخلات للمزود، وتنزيل المخرجات. تشمل المراجعة المستخدم والمؤسسة والدور والحساب والمشروع وحدود موافقة المالك، مع تحديث القيم القديمة المخزنة داخل جلسة قاعدة البيانات.

الطلب الذي سبق إرساله للمزود يحتفظ بمعرّفاته ويصبح بحاجة إلى مراجعة عند سحب الإذن؛ لا يُعاد تلقائيًا ولا يُزعم أنه أُلغي لدى المزود. لا يمكن استرجاع بيانات سبق تنزيلها أو إرسالها خارجيًا بمجرد تغيير إذن محلي.

## الأدلة

- أُثبتت الفجوة على المصدر السابق بخمسة اختبارات سلبية فاشلة، داخل بيئة معزولة بلا طلبات حقيقية للمزود.
- بعد الإصلاح: **46 اختبارًا ناجحًا**، منها **5 اختبارات PostgreSQL فعلية بين جلسات مستقلة** لإثبات سحب الإذن وتغيير نطاقه وتعليق المستخدم والمؤسسة والحساب.
- فحوص Ruff وMypy ناجحة. فحوص GitHub الاثنا عشر ناجحة، بما فيها Backend، الواجهات، CodeQL، أمن التبعيات والحاويات، والترقية مع الحفاظ على البيانات.
- فحص الصورة المنشورة بـTrivy 0.72.0 وقاعدة ثغرات أُنزِلت أثناء الدفعة: **صفر ثغرات HIGH/CRITICAL مرصودة**. هذه نتيجة نطاق الفحص، وليست ضمانًا بعدم وجود أي ثغرة ممكنة.
- اختبار على الكود المنشور: تنزيل مسموح = **200** مع مطابقة SHA-256 للمخرج السابق؛ بعد سحب الإذن داخل معاملة اختبار = **403**؛ ثم rollback ومراجعة مستقلة أثبتا عودة الإذن وعدم تغير سجل الصلاحيات الدائم.
- لا pytest على قاعدة الإنتاج، ولا إدخالات تنفيذ جديدة في الطابور، ولا طلبات مدفوعة جديدة للمزودين.

## النسخ والرجوع

النسخة الجديدة `703bb06a-acc8-4e42-bc66-65adb09251e6` اكتملت محليًا وفي Cloudflare R2؛ حجمها **20,859,924 بايت** وSHA-256:
`0c5b4e51e304fafd1e8b7c60c6fba92a39ec490b4dc35039c12080f0a5699a71`.

اختبار الاستعادة المعزول `cc68a0e2-f04a-4baa-97ca-827ae6861432` مكتمل؛ قاعدة البيانات والاستعادة من R2 وملفات 3D كلها مثبتة. لم تُنفذ استعادة فوق بيانات الإنتاج.

صورة الرجوع السابقة محفوظة بالوسم `aionex-aios-backend:rollback-pre-pr603-20260910`؛ لم نحتج إلى الرجوع.

## حالة الإنتاج بعد النشر

**40 حاوية إجمالًا: 36 عاملة و4 مهام تمهيدية منتهية بنجاح.** لا حاويات عاملة غير سليمة، ولا توقف غير متوقع، ولا إعادة تشغيل مسجلة. تغيرت صورة Backend وعامل Identity Media فقط؛ بقية صور الخدمات لم تتغير.

الواجهة العامة وواجهة البرمجة وفحص الجاهزية المحلي تعيد **200**. بوابات التشغيل الحالية **10/10 ناجحة**؛ هذه ليست نسبة إكمال كل الخصائص المؤجلة. مخطط قاعدة البيانات بقي `20260908_0046`، والطابور النشط للميزة = **0** عند التحقق النهائي.

## الحفاظ على الاتفاقات وما بقي

لم يتغير Cloudflare أو DNS، ولا اختبارات/حدود السعة المعتمدة، ولا قرارات تأجيل الحساب الثاني لـRunPod أو المزودين/المدفوعات/XR أو تشفير القرص لدى Namecheap. نُقل تقرير السعة القديم من PR #586 إلى main بنصه الأصلي ضمن #603 حتى لا يضيع بين المحادثات؛ لا يُخلط قبول 1000 اتصال تحكم مع 1000 بث خارجي متزامن.

**Voice Transform وFace Swap ما زالا بحاجة إلى تنفيذ وقبول تشغيلي مستقل.** تقليد الشخصيات العامة المرخصة لا يصبح متاحًا بمجرد موافقة المالك؛ بوابة الترخيص ما زالت مغلقة كما كانت.

## نقطة المتابعة

المرجع الآني الرسمي لهذه الدفعة:
`/opt/AIOS/.deployment-backups/identity-revocation-20260910/CURRENT-STATE.json`.

اقرأه قبل أي استنتاج من تسليم أقدم مكتوب قبل النشر. لا تُكرر النشر أو اختبارات المزود المدفوعة؛ راجع أولًا حالة طلب توثيق الإغلاق ثم انتقل إلى بند التنفيذ التالي من خارطة المشروع.
