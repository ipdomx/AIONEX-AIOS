# Phase 36N receipt — FR-05A

FR-05A defines the client-side encrypted R2 backup contract and separate recovery-key mount. AES-256-GCM envelope tests cover round-trip, wrong key, corruption, unsafe permissions, and production mount separation. Candidate only; R2 integration/deployment remains FR-05B/FR-05C.
