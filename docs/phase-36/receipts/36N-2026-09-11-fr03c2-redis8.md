# 36N / FR-03C2 — redis-py 8 runtime acceptance

The backend Redis client is proposed to move from redis-py 5.0.0 to 8.1.0. Acceptance used an isolated Redis 8 container and no production data.

The project admission Lua lease scripts, async connection lifecycle, and `RedisRealtimeBackplane` Pub/Sub path all completed against the isolated server. Focused Redis/realtime/auth tests passed 18/18, the repository Core suite passed 956/956, dependency resolution passed, and the runtime dependency audit reported no known vulnerabilities.

Database-backed tests that require PostgreSQL are not represented as local PASS; protected GitHub Backend CI remains mandatory. No production mutation is authorized by this receipt.
