# Phase 36C — Multi-provider / Model / Agent Execution Baseline Receipt

- Receipt ID: P36-R-36C-20260817
- Batch: 36C — Multi-provider/model/agent execution pools
- Date/time UTC: 2026-08-17T08:29:20Z
- Objective: Connect the production Project Cycle to AIOS's existing multi-provider routing capabilities without duplicating provider adapters, while enforcing organization/project/task isolation, durable routing evidence, provider health/rate/budget policy, approved failover, role separation, and local/private routing.
- Production gap before change: Phase 36B left production healthy with two same-host Project Workers and no active ProjectExecution jobs, but `web-dashboard/backend/app/services/project_execution.py` remains OpenAI-specific: 28 OpenAI literals, 8 allowed-OpenAI endpoint references and a fixed `gpt-5-mini` planning model are present. The Project Cycle does not consume the existing `MultiModelPlatform`/`AIRoutingLayer` or the durable `AIProvider` catalog.
- Changed paths/services/schemas: baseline/reporting plus the merged isolated route-plan foundation and the current durable authority source: `ProjectAIRoutePlanRecord`, `ProjectAIRouteTaskRecord`, `ProjectAIRouteAttemptRecord`, `ProjectAIExecutionBudget`, Alembic `20260817_0029`, `project_execution_routing_durable.py`, updated shipped-head contract, and durable authority tests. Migration `0029` has been exercised only on disposable PostgreSQL; Production remains on schema `0028` and no ProjectExecution worker/API/provider wiring is changed.
- Technology/version review and official sources: existing protocol choices were reviewed against current official provider documentation. OpenAI Responses, Anthropic Messages, Cohere v2 Chat, Azure OpenAI v1, AWS Bedrock Converse, Ollama Chat, OpenRouter Chat Completions, Mistral Chat Completions, Groq/Together/Fireworks/Hugging Face OpenAI-compatible chat remain supported. Gemini `generateContent` remains supported, but Google now recommends Interactions as the primary primitive for new agentic workflows; 36C must decide migration through compatibility tests rather than silently switching the production provider contract. Sources: https://platform.openai.com/docs/quickstart/make-your-first-api-request ; https://docs.anthropic.com/en/api/messages ; https://ai.google.dev/api ; https://docs.cohere.com/v2/reference/chat ; https://learn.microsoft.com/en-us/azure/foundry/openai/api-version-lifecycle ; https://docs.aws.amazon.com/bedrock/latest/userguide/conversation-inference.html ; https://docs.ollama.com/api/chat ; https://openrouter.ai/docs/api/api-reference/chat/send-chat-completion-request ; https://docs.mistral.ai/api/ ; https://console.groq.com/docs/openai ; https://docs.together.ai/docs/inference/openai-compatibility ; https://docs.fireworks.ai/tools-sdks/openai-compatibility ; https://huggingface.co/docs/inference-providers/en/tasks/chat-completion .
- Security/privacy/cost/sector review: production provider secrets remain encrypted/server-side and are never read by this work. The route-plan contract makes organization/workspace/project/execution scope immutable, requires an explicit tenant provider allowlist, rejects `model=default` as live evidence, and emits audit evidence without prompt/system-prompt text. Restricted data routes only to validated local models and cannot acquire a remote fallback. Existing durable `ScopedMemory` remains the required organization/project memory authority; the legacy project-only memory path is not wired. Shared distributed provider quota/circuit/budget state is still an open 36C gate. No provider call or spend is performed.
- Tests and exact pass counts: baseline evidence remains provider/routing `21/21 PASS`, Phase 29J provider/model/synthetic `45/45 PASS`, Phase 29F tenant-scoped memory `4/4 PASS`, Phase 36 root `15/15 PASS`, and route-plan foundation `7/7 PASS` / legacy+new routing `27/27 PASS`. Durable authority tests are `8/8 PASS`; unified route-plan/durable/legacy/head regression is `32/32 PASS`; migration round-trip `0029 -> 0028 -> 0029` PASS with route tables `4 -> 0 -> 4`; Backend compile/Ruff PASS and Mypy PASS across `181` source files; fresh Full Backend at Alembic `0029` is `657 passed, 0 failed` in `106.59s`. Disposable PostgreSQL logs show no connection exhaustion/deadlock/PANIC/FATAL and all four durable route tables return to zero rows after cleanup.
- Performance/load evidence: no live provider throughput benchmark is claimed. Real Redis/PostgreSQL disposable acceptance proves two independent authority instances share provider concurrency, fixed-window request rate and circuit state; Redis coordination keys contain only SHA-256 scope digests and opaque lease tokens; Redis acquisition failure fails closed. PostgreSQL micro-USD reservations are shared/durable, idempotent finalization does not double-spend, and spend from a failed primary can prevent an unaffordable fallback before provider execution. Production remains untouched.
- Problems discovered: P36-0010 is closed for the source/isolated routing-authority boundary: organization scope is immutable, route/audit/budget state is durable and shared rate/concurrency/circuit state is Redis-backed; live ProjectExecution integration remains pending under P36-0011. P36-0011 remains open because the live Project Cycle is still OpenAI-hardwired. P36-0012 is closed for the source routing authority: only connected tenant provider rows with current explicit `validated_models` evidence are eligible, `default` aliases are rejected and expired evidence is ignored; population/verification of real production provider model evidence remains an external/live acceptance gate.
- Root causes and why prior safeguards missed them: provider routing, durable provider management, enterprise memory and ProjectExecution were completed in different historical phases with separate acceptance scopes. Their unit/integration tests prove each subsystem independently but did not require the production Project Cycle to consume the shared provider catalog under distributed multi-tenant workers.
- Fixes and regression tests: durable routing authority implemented without provider credentials or network execution. `DurableProjectAIResolver` validates ProjectExecution tenant scope and resolves only organization-owned `connected`/enabled providers with current explicit validated model/pricing/capability/coordination evidence. `DurableProjectAIRouteStore` persists prompt-free plan/task/audit records and an integer micro-USD execution budget. `ProjectAISharedCoordinator` uses Redis server time plus hashed keys for cross-worker rate/concurrency/circuit state and idempotent finalization. `DurableProjectAIAuthority` reserves/settles budget transactionally, records prompt-free attempts, fails closed on shared coordination/budget errors and prevents duplicate spend on retry. Still pending: organization-scoped agent memory adapter into the Project Cycle, ProjectExecution worker wiring, provider-specific live model evidence, and controlled live provider acceptance.
- PR / merge SHA / protected checks: Phase 36C starts from PR #393 merge `fc589766adb677d2a9510344997fc948cae1a030`. Baseline PR #394=`918c846fd18838c7cbd77d1f3e3d5d2d4b4385ee`, baseline evidence PR #395=`6704b5fba7b7fefa967b96d227993930f2cf4769`, route-plan PR #396=`0e9308c3f379cce86ca42eab53aa2c36c1364ca0`, and route-plan evidence PR #397=`e9dd0b64038ade8fe7a996f07bc388b47df4cc89` are merged with protected checks green. The current `phase36c/durable-routing-authority` branch has no PR yet and must pass the same protected gates before merge.
- Backup / deployment / rollback: Production is unchanged by this durable-authority checkpoint; therefore no production backup/deploy is performed. Alembic `0029` is verified from zero and by `0029 -> 0028 -> 0029` only on disposable PostgreSQL. Source rollback is branch/commit revert. Any later production application of `0029` must create a fresh backup/restore checkpoint before migration, and ProjectExecution/provider wiring must retain a rollback path to the current OpenAI execution path until parity is proven.
- Live acceptance and before/after metrics: no live provider call or production mutation is made. Production remains on the accepted Phase 36B runtime/schema `0028`, with the previously inventoried 15 AI provider rows (13 connected, Azure OpenAI/AWS Bedrock configured) and no claim that those rows currently contain the new `validated_models` evidence contract. The durable authority is proven only against deterministic/disposable PostgreSQL+Redis at this checkpoint.
- External activation gates remaining: apply migration `0029` only after a fresh production backup/restore gate; populate/verify provider-specific non-placeholder model/capability/pricing evidence; Azure OpenAI/AWS Bedrock require successful live connection/execution evidence; wire organization/project scoped memory; integrate the durable authority into ProjectExecution while retaining rollback compatibility; prove distributed fake-provider execution through both Workers before any live provider call; no fallback may cross tenant/privacy/locality/budget policy; Gemini Interactions adoption remains an explicit technology decision.
- Next action: run final Phase36/reporting/security/static/full-backend review on this durable authority change, commit/push/open a protected PR, and merge only when all checks are green. After merge, resume with organization-scoped agent memory + ProjectExecution worker integration using deterministic fake providers first. Do not apply migration `0029` to Production or make a live provider request until that integrated path is green and a fresh backup/deployment gate is recorded.
