# Phase 36C — Multi-provider / Model / Agent Execution Baseline Receipt

- Receipt ID: P36-R-36C-20260817
- Batch: 36C — Multi-provider/model/agent execution pools
- Date/time UTC: 2026-08-17T08:29:20Z
- Objective: Connect the production Project Cycle to AIOS's existing multi-provider routing capabilities without duplicating provider adapters, while enforcing organization/project/task isolation, durable routing evidence, provider health/rate/budget policy, approved failover, role separation, and local/private routing.
- Production gap before change: Phase 36B left production healthy with two same-host Project Workers and no active ProjectExecution jobs, but `web-dashboard/backend/app/services/project_execution.py` remains OpenAI-specific: 28 OpenAI literals, 8 allowed-OpenAI endpoint references and a fixed `gpt-5-mini` planning model are present. The Project Cycle does not consume the existing `MultiModelPlatform`/`AIRoutingLayer` or the durable `AIProvider` catalog.
- Changed paths/services/schemas: protected route-plan/durable/integration/runner-boundary work is already live at schema `0029` with runner `legacy`. The current provider-evidence source adds `provider_model_evidence.py` plus deterministic tests only; no Production provider row, env file, credential, runner mode or service is changed by this source gate.
- Technology/version review and official sources: existing protocol choices were reviewed against current official provider documentation. OpenAI Responses, Anthropic Messages, Cohere v2 Chat, Azure OpenAI v1, AWS Bedrock Converse, Ollama Chat, OpenRouter Chat Completions, Mistral Chat Completions, Groq/Together/Fireworks/Hugging Face OpenAI-compatible chat remain supported. Gemini `generateContent` remains supported, but Google now recommends Interactions as the primary primitive for new agentic workflows; 36C must decide migration through compatibility tests rather than silently switching the production provider contract. Sources: https://platform.openai.com/docs/quickstart/make-your-first-api-request ; https://docs.anthropic.com/en/api/messages ; https://ai.google.dev/api ; https://docs.cohere.com/v2/reference/chat ; https://learn.microsoft.com/en-us/azure/foundry/openai/api-version-lifecycle ; https://docs.aws.amazon.com/bedrock/latest/userguide/conversation-inference.html ; https://docs.ollama.com/api/chat ; https://openrouter.ai/docs/api/api-reference/chat/send-chat-completion-request ; https://docs.mistral.ai/api/ ; https://console.groq.com/docs/openai ; https://docs.together.ai/docs/inference/openai-compatibility ; https://docs.fireworks.ai/tools-sdks/openai-compatibility ; https://huggingface.co/docs/inference-providers/en/tasks/chat-completion .
- Security/privacy/cost/sector review: production provider secrets remain encrypted/server-side and are never read by this work. The route-plan contract makes organization/workspace/project/execution scope immutable, requires an explicit tenant provider allowlist, rejects `model=default` as live evidence, and emits audit evidence without prompt/system-prompt text. Restricted data routes only to validated local models and cannot acquire a remote fallback. Existing durable `ScopedMemory` remains the required organization/project memory authority; the legacy project-only memory path is not wired. Shared distributed provider quota/circuit/budget state is still an open 36C gate. No provider call or spend is performed.
- Tests and exact pass counts: retained Gate 2 evidence remains selector `5/5`, unified runner/routing regression `48/48`, Full Backend `667 passed, 1 skipped`, Phase36 root `16/16`. Provider-model evidence focused tests are `6/6 PASS`; provider-evidence + route-plan + durable authority + ProjectExecution integration + runner boundary + Phase29J runtime regression is `39/39 PASS`; Backend Ruff PASS; Mypy PASS across `183` source files; fresh Full Backend on disposable PostgreSQL 16 + Redis 7 at Alembic `0029` is `673 passed, 1 skipped, 0 failed` in `110.26s`. Disposable Project-AI route/task/attempt/budget rows cleaned to zero and PostgreSQL/Redis critical log hits were `0`.
- Performance/load evidence: no live provider throughput benchmark is claimed. At the ProjectExecutionWorker layer, two concurrent workers in different organizations complete independent provider plans with project-scoped memory isolation; two workers in the same organization sharing an OpenAI model with `concurrent_requests=1` observe the same Redis authority, causing exactly one execution to retain OpenAI and the other to use approved Anthropic fallback while both complete. Existing retry semantics remain unchanged: a no-fallback provider failure returns the execution to `retry_queued`. No production provider/network call is made.
- Problems discovered: P36-0010 is closed through deterministic/local ProjectExecution integration: organization scope, durable audit/budget, shared cross-worker coordination and project-scoped memory are exercised together. P36-0011 is closed for deterministic/local integration but remains an explicit Production activation gate because the default live worker still uses `ProjectPlanningRunner`/OpenAI until controlled rollout. P36-0012 remains source-closed/live-evidence-pending: only current explicit `validated_models` can route; production provider model evidence has not yet been populated/live-verified. One reused disposable PostgreSQL instance developed test-credential drift during a chained rerun; it was destroyed/recreated before the retained evidence run, with no Production effect or source workaround. P36-0013: the existing integration regression asserted the literal legacy constructor source string, so Gate 2 correctly broke that stale text assertion even though runtime behavior remained legacy by default; the regression now asserts the selector-based legacy boundary instead of an obsolete implementation detail. P36-0014: Production began with 15 AI provider rows and every row `validated_models=0`. After source protection, inventory-only live proof succeeded for all 11 connected inventory-capable providers and is retained outside Git; Production rows remain deliberately unchanged until reviewed model policy exists. Anthropic/Cohere/AWS Bedrock correctly require execution as authoritative model proof and Azure OpenAI remains configured/not-connected. Therefore no Phase36C runner activation is allowed yet.
- Root causes and why prior safeguards missed them: provider routing, durable provider management, enterprise memory and ProjectExecution were completed in different historical phases with separate acceptance scopes. Their unit/integration tests prove each subsystem independently but did not require the production Project Cycle to consume the shared provider catalog under distributed multi-tenant workers.
- Fixes and regression tests: added `ProjectAIProjectMemoryAdapter`, which verifies exact organization/workspace/project/execution/requester scope and persists only project-scoped `ScopedMemory` plus prompt-free audit metadata; added `DeterministicProjectAIIntegrationRunner`, which preserves the existing synchronous Worker runner contract, creates durable route plans, obtains shared Redis/PostgreSQL attempt permits, uses only injected provider invokers, records success memory, performs approved fallback, and returns a Worker-compatible summary. `ProjectExecutionWorker.complete()` now persists the runner-reported provider when present; the existing OpenAI runner already reports OpenAI, so rollback/default behavior is unchanged. Regression tests prove two-org isolation, same-org/project memory separation, shared provider concurrency/fallback, no-fallback retry behavior, prompt exclusion and default-runner preservation. Gate 2 adds a fail-closed runner selector. The current provider-evidence gate adds provider-specific inventory parsing, explicit inventory-vs-execution evidence classes, versioned `ProviderModelValidationSpec`, stale/future/default rejection, bounded TTL, tenant-scoped idempotent persistence to `config.validated_models`, and prompt/credential-free audit metadata. Inventory requests are dependency-injected in tests; no static catalogue score/model alias is accepted as live proof and Anthropic/Cohere/Bedrock cannot be falsely marked inventory-validated.
- PR / merge SHA / protected checks: Phase 36C starts from PR #393=`fc589766adb677d2a9510344997fc948cae1a030`. Baseline PR #394=`918c846fd18838c7cbd77d1f3e3d5d2d4b4385ee`, baseline evidence PR #395=`6704b5fba7b7fefa967b96d227993930f2cf4769`, route-plan PR #396=`0e9308c3f379cce86ca42eab53aa2c36c1364ca0`, route-plan evidence PR #397=`e9dd0b64038ade8fe7a996f07bc388b47df4cc89`, durable-authority PR #398=`789b63c215f4ceeb613d13f08b707e9465536d39`, durable merge-evidence PR #399=`e485acf50ea589017bdba5998eac1323c6aeb614`, evidence-correction PR #400=`8342e42b51ef0a04d70a1671d16df496d8a97884`, deterministic ProjectExecution integration PR #401=`451e2e40fd0d5bb790c884077090dac126bef8d9`, integration merge-evidence PR #402=`91fe417956c047dd68958d3400d89230abc1f434`, pre-production checkpoint PR #403=`3f49e3e9889542afb02598cb6178288191db8024`, and backup/restore evidence PR #404=`11651979c9779305d300c2bdf385419512d1ae1d` are merged with protected checks green; Gate 2 runner/rollback boundary PR #405=`c01ce06770bac00897673664e47dbd406201eeaf` and Production schema activation evidence PR #406=`130e4ff3cefbf5dff11305128a25c786e7e28b6d` are merged with every protected check green. Provider-model evidence PR #407=`36fcee5e001ccfc08123f1b0a6f97627229b81ef` is merged with all protected checks green. No live provider inventory request had been made before this merge checkpoint.
- Backup / deployment / rollback: fresh pre-activation backup `/opt/AIOS/.deployment-backups/phase36c-production-activation/aios-20260817T115113Z.tar.gz` was created from live `0028` with SHA-256 `bb75913df3c38962e698fd46c886260ed8cdfaf2b0a3806b5e7d498042d8da3`; disposable restore smoke matched Alembic `0028`, executions `1`, providers `15`, backup records `13`, organizations `2`. Gate 2 PR #405 merged as `c01ce06770bac00897673664e47dbd406201eeaf`. Rollback images preserve the pre-36C Backend/Worker IDs. Production migration `0028 -> 0029` then completed successfully, Backend and both Project Workers were recreated on the merged Gate 2 images, and both Workers are explicitly pinned `PROJECT_EXECUTION_RUNNER_MODE=legacy`. Schema rollback remains available through the verified backup plus source/image rollback; runner rollback requires no schema downgrade because legacy remains active.
- Live acceptance and before/after metrics: Production schema is `0029` while multi-provider execution remains deliberately inactive. Backend/two Project Workers remain healthy with runner mode `legacy`, Project AI route state and active ProjectExecution jobs remain zero. Gate 3 ingress remained HTTP 200 with API `/ready` p95 `147.4ms`. After protected provider-evidence PR #407, inventory-only authenticated model-list probes were executed one provider at a time with no generation/spend and no provider-row writes. Model counts: OpenAI `124`, Gemini `50`, OpenRouter `414`, Ollama `1`, Mistral `55`, xAI `12`, DeepSeek `2`, Groq `13`, Together `281`, Fireworks `24`, Hugging Face `136`. Evidence files are stored outside Git under `.deployment-backups/phase36c-provider-inventory/`; aggregate SHA-256 `71d04d52ce62f0e12d0489dca14d75ca4c03874214b7785ed3b826ec63b1b3cb`. Production still has zero persisted validated models after the inventory round.
- External activation gates remaining: backup/restore, migration `0029`, runner rollback control and live inventory are complete. Remaining launch gates are reviewed/persisted current provider-model evidence; tenant-safe platform-provider Owner policy; Free-local/Paid-validated/per-user routing; provider credit/quota Owner alerts; production invoker/error mapping; bounded provider live acceptance; Launch-100 admission/routing acceptance; production two-Worker route/fallback/memory acceptance; no fallback may cross tenant/privacy/locality/budget policy; Azure OpenAI/AWS Bedrock remain external connection/execution gates; Gemini Interactions remains an explicit technology decision.
- Next action: protect the Launch-100 access/finance foundation first. Free defaults to the local zero-provider-cost `ollama:gemma3:4b`; paid access remains fail-closed until the Owner selects fresh validated provider/model evidence, with per-user override available. Then persist a reviewed initial model set, build the live invoker/error mapping, prove provider credit alerts, run 100-user durable launch acceptance, and only then perform bounded Phase36C live canary/two-Worker acceptance. Production remains `legacy` until those gates are green.

- Launch-100 scope added by Owner directive: first release must durably admit at least 100 simultaneous users/project submissions while retaining the 1000-admission architecture for later expansion. Free users must route only to Owner-approved local/free models; paid users route only to Owner-approved paid validated models; per-user Owner override has highest policy priority.
- Platform provider pool boundary: credentials stay server-side under the platform provider organization; explicit Owner policy grants a consumer tenant access to exact provider/model routes without copying provider secrets.
- Provider finance/alert boundary: Owner-funded credit and warning/critical thresholds are tracked in micro-USD against durable actual provider spend; periodic and immediate quota/billing signals use the existing platform-owner notification channels.
- OpenAI freshness note: official OpenAI guidance currently identifies the GPT-5.6 family as current, but the authenticated project credential inventory did not contain GPT-5.6 IDs at the live inventory checkpoint. The system therefore refuses to hard-code an unavailable model and will notify the Owner that the desired-current model is unavailable until fresh credential-specific evidence proves an executable ID.

- Launch-100 Owner UI checkpoint: `/owner/project-ai` now exposes plan/user routing policy and provider finance controls using safe snapshot data. Managed-user rows exclude password/credential fields. Frontend gates: TypeScript PASS, ESLint PASS, Arabic coverage `963/5`, Prettier PASS, production build `87/87`; backend Launch/Finance `13/13 PASS` plus Owner route/client contract PASS.

- Launch-100 acceptance evidence: 100 isolated user/org/project executions completed with exact Free/Paid/User-override routing and project-scoped memory. 4-slot baseline: elapsed `60.928s`, p50 `31.202s`, p95 `58.301s`, max `60.322s`. 6-slot run: elapsed `60.226s`, p50 `32.372s`, p95 `58.096s`, max `59.699s`. Both runs were `100/100` with no network/provider spend. A source-only 3-replica x capacity-2 launch override is prepared but not deployed; the small 4->6 slot latency delta does not justify more same-host replicas without additional evidence.

### 36C Launch-100 acceptance checkpoint — 2026-08-17

- Protected-source branch: `phase36c/launch-100-readiness`; Launch foundation commits `1fb1387` (access/provider-finance), `43b3948` (Owner Project-AI controls) and `17c602a` (Launch-100 acceptance test). PR #409 is open against `main`; Production is not changed by this PR and remains Alembic `0029` with both Project Workers in `legacy` runner mode.
- Isolated Launch-100 acceptance PASS: `100` users / `100` organizations / `100` projects admitted and completed through `4` bounded Project Workers. Routing distribution was `50` Free -> `ollama:gemma3:4b`, `40` Paid -> Mistral test route, `10` Owner override -> DeepSeek test route. No queued/running execution remained after drain; exactly `100` durable route attempts and `100` project-scoped memory records were verified with unique tenant/project scope.
- Timing evidence: elapsed `60.527s`; completion p50 `31.376s`, p95 `57.990s`, max `59.910s`. This is isolated deterministic launch evidence, not a claim of 100 simultaneous external-provider generations. The production design keeps execution concurrency bounded while durable admission/queueing supports the launch population.
- The acceptance test is deterministic/fake-provider only. It proves tenant isolation, Free/Paid/Owner-override routing, bounded four-worker drain, durable attempt accounting and memory isolation without provider spend. Live-provider acceptance and production runner activation remain separate gates.
- Current PR #409 correction evidence: stale Owner navigation page-count contract was updated from 46 to 47 after adding `/owner/project-ai`; the exact Owner navigation contract is `1/1 PASS`. A fresh isolated Full Backend run on the corrected working tree is `691 passed, 1 skipped, 0 failed` in `177.46s`.
- Reviewed launch-model policy evidence: current credential-specific inventories contain `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `mistral-medium-3-5`, and `deepseek-v4-pro`; OpenAI/Mistral/DeepSeek policy values are kept separate from existence evidence and routing still requires fresh validated-model persistence. Focused launch-model/access/integration/navigation regression is `19/19 PASS`; model-policy unit tests are `4/4 PASS`; Ruff/Mypy focused checks PASS.
- Current safe next action: push the PR #409 correction/model-policy follow-up, let every protected check rerun, and merge only when green. Production remains Alembic `0029` with both Project Workers on `legacy`; no provider model persistence or runner switch belongs to this source correction.

- Current-model live-runtime refresh checkpoint: authenticated OpenAI re-probe now directly confirms GPT-5.6 Sol/Terra/Luna on the project credential. Reviewed launch choices are Free `ollama:gemma3:4b` and Paid GPT-5.6 Sol/Terra/Luna, Mistral Medium 3.5, DeepSeek V4 Pro, all still requiring fresh validated evidence. Live runtime is double-armed/fail-closed and reuses existing provider adapters. Owner manual evidence refresh is wired at `/owner/project-ai/models/refresh`; refresh contracts `3/3 PASS`, focused live-runtime/model/access regression `27/27 PASS`, frontend TypeScript/ESLint/Arabic `965/5`/Prettier/build `87/87` PASS. Production remains `0029 + legacy`.

- Automatic current-model refresh source: disabled-by-default `PROJECT_AI_MODEL_REFRESH_ENABLED=false`, 4-hour bounded observer cadence, separate committed refresh session and notification publishing; scheduler/Phase29G observer regression `10/10 PASS`. Launch gate P36-0015 remains open because the legacy rollback Compose path still references `gpt-5.4-nano`; Phase36C live routing does not use it, but final launch requires replacing that legacy model with credential-proven GPT-5.6 evidence and revalidating rollback.
