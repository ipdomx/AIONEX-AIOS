# Phase 36C — Multi-provider / Model / Agent Execution Baseline Receipt

- Receipt ID: P36-R-36C-20260817
- Batch: 36C — Multi-provider/model/agent execution pools
- Date/time UTC: 2026-08-17T08:29:20Z
- Objective: Connect the production Project Cycle to AIOS's existing multi-provider routing capabilities without duplicating provider adapters, while enforcing organization/project/task isolation, durable routing evidence, provider health/rate/budget policy, approved failover, role separation, and local/private routing.
- Production gap before change: Phase 36B left production healthy with two same-host Project Workers and no active ProjectExecution jobs, but `web-dashboard/backend/app/services/project_execution.py` remains OpenAI-specific: 28 OpenAI literals, 8 allowed-OpenAI endpoint references and a fixed `gpt-5-mini` planning model are present. The Project Cycle does not consume the existing `MultiModelPlatform`/`AIRoutingLayer` or the durable `AIProvider` catalog.
- Changed paths/services/schemas: baseline/reporting plus the isolated route-plan foundation `web-dashboard/backend/app/services/project_execution_routing.py` and `web-dashboard/backend/tests/test_phase36c_project_execution_routing.py`. No schema, live ProjectExecution wiring, provider configuration, production service, secret, provider call, project job or user workload is changed.
- Technology/version review and official sources: existing protocol choices were reviewed against current official provider documentation. OpenAI Responses, Anthropic Messages, Cohere v2 Chat, Azure OpenAI v1, AWS Bedrock Converse, Ollama Chat, OpenRouter Chat Completions, Mistral Chat Completions, Groq/Together/Fireworks/Hugging Face OpenAI-compatible chat remain supported. Gemini `generateContent` remains supported, but Google now recommends Interactions as the primary primitive for new agentic workflows; 36C must decide migration through compatibility tests rather than silently switching the production provider contract. Sources: https://platform.openai.com/docs/quickstart/make-your-first-api-request ; https://docs.anthropic.com/en/api/messages ; https://ai.google.dev/api ; https://docs.cohere.com/v2/reference/chat ; https://learn.microsoft.com/en-us/azure/foundry/openai/api-version-lifecycle ; https://docs.aws.amazon.com/bedrock/latest/userguide/conversation-inference.html ; https://docs.ollama.com/api/chat ; https://openrouter.ai/docs/api/api-reference/chat/send-chat-completion-request ; https://docs.mistral.ai/api/ ; https://console.groq.com/docs/openai ; https://docs.together.ai/docs/inference/openai-compatibility ; https://docs.fireworks.ai/tools-sdks/openai-compatibility ; https://huggingface.co/docs/inference-providers/en/tasks/chat-completion .
- Security/privacy/cost/sector review: production provider secrets remain encrypted/server-side and are never read by this work. The route-plan contract makes organization/workspace/project/execution scope immutable, requires an explicit tenant provider allowlist, rejects `model=default` as live evidence, and emits audit evidence without prompt/system-prompt text. Restricted data routes only to validated local models and cannot acquire a remote fallback. Existing durable `ScopedMemory` remains the required organization/project memory authority; the legacy project-only memory path is not wired. Shared distributed provider quota/circuit/budget state is still an open 36C gate. No provider call or spend is performed.
- Tests and exact pass counts: baseline evidence remains provider/routing `21/21 PASS`, Phase 29J provider/model/synthetic contracts `45/45 PASS`, Phase 29F tenant-scoped memory `4/4 PASS`, and Phase 36 root `15/15 PASS`. Route-plan foundation tests are `7/7 PASS`; legacy provider/routing plus the new contract are `27/27 PASS`. Focused Ruff PASS and Mypy PASS for `project_execution_routing.py`. Retained pytest warnings are read-only cache/deprecation warnings only. Full protected CI remains required before merge.
- Performance/load evidence: no live/load benchmark is added because the new executor uses only explicitly injected deterministic transports and is not connected to Production. The acceptance adapter proves approved primary/fallback ordering and fail-closed no-fallback behavior without network access. Production remains on the accepted Phase 36B two-worker runtime; distributed provider rate/budget coordination remains unproven and is an explicit 36C gate.
- Problems discovered: P36-0010 production integration boundary is not tenant/durability complete; P36-0011 the live Project Cycle is still OpenAI-hardwired despite an existing provider router; P36-0012 the source routing catalog uses many generic `default` model identifiers and static quality/latency/privacy/cost scores, so it cannot be treated as authoritative live model/pricing evidence without provider-specific validation.
- Root causes and why prior safeguards missed them: provider routing, durable provider management, enterprise memory and ProjectExecution were completed in different historical phases with separate acceptance scopes. Their unit/integration tests prove each subsystem independently but did not require the production Project Cycle to consume the shared provider catalog under distributed multi-tenant workers.
- Fixes and regression tests: route-plan foundation implemented. `ProjectAIScope` binds organization/workspace/project/execution; validated runtime model records reject `default`; tenant policy requires an explicit allowlist and supports blocked/offline/privacy/budget constraints; each task/role receives a prompt-free primary/fallback route with exact provider/model/cost/reasons/evidence reference; the deterministic injected-transport adapter proves approved fallback and no-fallback fail-closed behavior. Still pending: durable tenant provider/model resolver, shared quota/circuit/budget authority, persistent route/audit records, organization-scoped agent memory adapter, and actual ProjectExecution wiring.
- PR / merge SHA / protected checks: Phase 36C starts from PR #393 merge `fc589766adb677d2a9510344997fc948cae1a030`. Baseline/inventory PR #394 merged as `918c846fd18838c7cbd77d1f3e3d5d2d4b4385ee`; baseline merge-evidence PR #395 merged as `6704b5fba7b7fefa967b96d227993930f2cf4769`. Route-plan foundation PR #396 merged into `main` as `0e9308c3f379cce86ca42eab53aa2c36c1364ca0` after every required protected check passed, including Backend Tests, Production Docker Build, Browser boundaries, Frontend Build, CodeQL, repository secret/hygiene, SBOM, Dependency Security, Core contracts and Phase 36 Reporting.
- Backup / deployment / rollback: production is unchanged; no new backup is required for this isolated source/test checkpoint. Source rollback is revert/removal of the route-plan foundation. Any later 36C schema/runtime deployment must establish its own backup/rollback gate before mutation.
- Live acceptance and before/after metrics: production non-secret inventory shows 15 AI provider rows: 13 `connected` and 2 `configured` (Azure OpenAI and AWS Bedrock); 0 durable `ai_agents` rows; 0 active ProjectExecution jobs. Tripo3D/Meshy are separate catalog-only 3D connector types and are not counted as 36C AI execution providers.
- External activation gates remaining: Azure OpenAI and AWS Bedrock require successful live connection/execution evidence before routing; provider-by-provider model IDs/capabilities/pricing must be validated rather than using generic placeholders; real distributed provider quota/rate coordination must be proven; no fallback may cross tenant/privacy/locality policy; Gemini Interactions adoption remains an explicit technology decision rather than an automatic migration.
- Next action: route-plan foundation is protected and merged. Resume only with the durable organization-scoped provider/model resolver, persistent route/audit evidence, and shared distributed quota/circuit/budget coordination using deterministic fake providers first. Do not wire live ProjectExecution provider calls or mutate production until those isolation/audit/fallback/rate-budget regressions are green and this receipt is updated.
