# Phase 36 receipt — FR-04C1 hard-link backup policy

FR-04C1 adds a fail-closed hard-link guard to protected platform asset snapshots. It is a source-level consistency and path-safety improvement only.
