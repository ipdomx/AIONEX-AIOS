"use client";
import {useEffect,useState} from "react";import {opsSecurityServices,DatabaseRow} from "@/lib/ops-security-services";import {LiveDataPanel,JsonCard} from "@/components/system/LiveDataPanel";
export default function Page(){const[d,setD]=useState<DatabaseRow[]>([]),[e,setE]=useState<string|null>(null),[l,setL]=useState(true);useEffect(()=>{opsSecurityServices.databases().then(setD).catch(x=>setE(x.message)).finally(()=>setL(false))},[]);return <LiveDataPanel title="Databases" subtitle="Live PostgreSQL and Redis inventory." loading={l} error={e} empty={!d.length}><div className="grid gap-4 xl:grid-cols-2">{d.map(x=><JsonCard key={x.id} title={String(x.name||x.id)} value={x}/>)}</div></LiveDataPanel>}
