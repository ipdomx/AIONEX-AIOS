"use client";
import {useEffect,useState} from "react";import {opsSecurityServices,PolicyRow} from "@/lib/ops-security-services";import {LiveDataPanel,JsonCard} from "@/components/system/LiveDataPanel";
export default function Page(){const[d,setD]=useState<PolicyRow[]>([]),[e,setE]=useState<string|null>(null),[l,setL]=useState(true);useEffect(()=>{opsSecurityServices.policies().then(setD).catch(x=>setE(x.message)).finally(()=>setL(false))},[]);return <LiveDataPanel title="Security Policies" subtitle="Enforced and owner-governed security policies." loading={l} error={e} empty={!d.length}><div className="grid gap-4 xl:grid-cols-2">{d.map(x=><JsonCard key={x.id} title={x.name} value={x}/>)}</div></LiveDataPanel>}
