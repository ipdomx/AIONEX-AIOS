# Never commit operator-managed credentials from this directory.
# Firebase Admin service-account JSON is mounted from the server at runtime.
# Provision runtime secrets only through the server or an approved secrets manager.
*
!.gitignore
