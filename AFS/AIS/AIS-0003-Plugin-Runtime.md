# AIS-0003: Plugin Runtime

Plugins declare an API version and capabilities. Production installations require a trusted signature. A plugin receives only explicitly granted capabilities and cannot import internal modules as an integration mechanism.
