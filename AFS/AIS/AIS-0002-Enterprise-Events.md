# AIS-0002: Enterprise Events

Events must be validated before publication, tenant-scoped, idempotent, auditable, and delivered only to declared consumers. Event payloads must never contain raw secrets.
